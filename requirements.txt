# Core dependencies
pandas>=1.3.0
openpyxl>=3.0.0

# Salesforce metadata service dependencies
simple-salesforce>=1.12.0
requests>=2.28.0
flask>=2.0.0
werkzeug>=2.0.0

# Note: argparse is part of the Python standard library since Python 3.2,
# so it's not included as a separate dependency for Python 3.8+ environments.

# For future enhancements, uncomment as needed
# spacy>=3.0.0
# transformers>=4.0.0
# torch>=1.9.0
# numpy>=1.20.0
